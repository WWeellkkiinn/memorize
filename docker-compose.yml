services:
  memorize:
    build: .
    ports:
      - "8881:8881"
    volumes:
      - .:/app
      - ./memorize-db:/data/memorize
    environment:
      - AUTH_USER=your_username
      - AUTH_PASS=your_password
    restart: unless-stopped
